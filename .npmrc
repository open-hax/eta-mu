# packages/legacy/github must install standalone in CI (`--ignore-workspace`), so
# its eta-mu-ai / eta-mu-cli dependencies are declared as published ranges rather
# than `workspace:*`. Linking keeps those ranges resolving to the local packages
# inside the monorepo, so a standalone install and a workspace install agree.
link-workspace-packages=true
