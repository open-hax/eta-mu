# Build artifacts
dist/
dist-dev/
dist-cljs/
build/
lib/
!packages/extensions/lib/
!packages/extensions/lib/**
!packages/extensions/src/eta_mu/build/
!packages/extensions/src/eta_mu/build/**
*.tsbuildinfo
**/coverage/

# Dependencies
node_modules/
*.log
*.pid
*.seed
*.pid.lock

# Environment files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# Shadow-CLJS build caches
**/.shadow-cljs/

# Runtime/build caches
**/.cache/

# Nested worktrees / standalone prototypes
/alpha/

# Local tool state
**/.cpcache/
**/.serena/
**/.clj-kondo/.cache
**/.clj-kondo/imports
**/.lsp/

# Playwright artifacts
packages/legacy/kanban/test-results/
packages/legacy/kanban/playwright-report/


**/target/

# TypeScript line-count baseline (local state)
.ts-line-count-baseline

# Generated OpenCode host artifacts
.opencode/

# Bounded review context, downloaded into the workspace by the MiMo reviewer.
# Its contents are checksum-verified against SHA256SUMS, so it is expected dirt
# rather than drift — but the workspace-clean guard in
# .github/workflows/opencode-code-review.yml counts untracked files, so it has
# to be ignored or that guard fails on the reviewer's own artifact.
.review-context/

# Core dumps
core

packages/eta-mu/dist-cli/
packages/eta-mu/*.tgz

.ημ/receipt-river/repository-inventory.edn
