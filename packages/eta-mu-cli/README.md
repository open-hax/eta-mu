# @open-hax/eta-mu-cli

Opinionated eta-mu CLI distribution for the absorbed Pi coding agent runtime.

This package starts as a thin `eta-mu` binary wrapper around the workspace-owned
`@open-hax/eta-mu-coding-agent` fork. The wrapper lets eta-mu diverge without
renaming the absorbed Pi packages immediately.
