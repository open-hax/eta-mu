{:paths ["src" "test"
         "../protocols/src"
         "../chat-ui/src"]

 :deps
 {thheller/shadow-cljs         {:mvn/version "3.4.10"}
  lilactown/helix              {:mvn/version "0.2.2"}
  metosin/malli                {:mvn/version "0.16.4"}
  ;; Katamorph owns the condition kernel; Rheos consumes it. Pinned by sha the
  ;; way packages/sol/deps.edn is, and awaiting the same treatment: sol carries
  ;; :git/tag alongside, which is what keeps a pin resolvable after the branch
  ;; it came from is gone. This sha is open-hax/katamorph#7, so it needs
  ;; re-pinning to a tagged main commit before this merges.
  io.github.open-hax/katamorph {:git/sha "e6b925ffdea66ce73d85de21cf18336cdf80fa07"}}}
