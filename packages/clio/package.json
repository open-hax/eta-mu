{
  "name": "@eta-mu/clio",
  "version": "0.1.0",
  "description": "Content-addressed event sourcing kernel for Clojure, ClojureScript and NBB",
  "type": "module",
  "license": "LGPL-3.0-or-later",
  "bin": {
    "clio": "./bin/clio.mjs"
  },
  "files": [
    "bin",
    "src",
    "deps.edn",
    "nbb.edn",
    "README.md"
  ],
  "dependencies": {
    "fs-ext-extra-prebuilt": "2.2.9",
    "nbb": "1.5.212"
  },
  "scripts": {
    "test": "pnpm run test:bb && pnpm run test:jvm && pnpm run test:nbb && pnpm run test:shadow",
    "test:jvm": "clojure -M:test",
    "test:bb": "bb test/run.bb",
    "test:nbb": "nbb -cp test:scripts test/run.nbb",
    "test:shadow": "pnpm exec shadow-cljs compile test",
    "lint": "pnpm run lint:kondo",
    "lint:kondo": "clj-kondo --lint src test bin scripts && bb scripts/lint_extern_boundary.bb",
    "clean": "rm -rf target .nbb"
  },
  "devDependencies": {
    "shadow-cljs": "3.4.11"
  }
}
